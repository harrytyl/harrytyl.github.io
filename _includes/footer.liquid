{% capture footer_contents %}
  &copy; Copyright {{ site.time | date: '%Y' }}
  {{ site.first_name }}
  {{ site.middle_name }}
  {{ site.last_name }}. {{ site.footer_text }}
  {% if site.impressum_path %}
    <a href="{{ site.url }}{{ site.baseurl }}{{ site.impressum_path }}">Impressum</a>.
  {% endif %}
  {% if site.last_updated %}
    Last updated: {{ 'now' | date: '%B %d, %Y' }}.
  {% endif %}
{% endcapture %}

{% if site.footer_fixed %}
  <footer class="fixed-bottom" role="contentinfo">
    <div class="container mt-0">
      {{ footer_contents }}
    </div>
  </footer>
{% else %}
  <footer class="sticky-bottom mt-5" role="contentinfo">
    {% if site.newsletter.enabled %}
      {% if site.plugins contains 'al_newsletter' %}
        {% include plugins/al_newsletter_form_center.liquid %}
      {% else %}
        {% include newsletter.liquid %}
      {% endif %}
    {% endif %}

    <div class="container">
      {{ footer_contents }}
    </div>
  </footer>
{% endif %}

{% comment %}
  Obfuscated email icon.

  The address is kept out of _data/socials.yml because jekyll-socials writes
  "mailto:<address>" verbatim, leaving the plain address in the HTML for
  harvesters. Here it is percent-encoded with encode_email, which browsers
  decode transparently, and the icon is appended to the page's .contact-icons
  row so the about and CV pages keep the same set of icons as before.
{% endcomment %}
{% if site.email %}
  <template id="contact-email-template">
    <a href="mailto:{{ site.email | encode_email }}" title="Email"><i class="fa-solid fa-envelope"></i></a>
  </template>
  <script>
    (function () {
      var tpl = document.getElementById('contact-email-template');
      if (!tpl) return;
      document.querySelectorAll('.contact-icons').forEach(function (row) {
        row.appendChild(tpl.content.cloneNode(true));
      });
    })();
  </script>
{% endif %}

{% comment %}
  Local override of al_folio_core's _includes/footer.liquid: identical to the
  gem template above, plus the analytics beacon below.

  footer.liquid is included unconditionally by default.liquid and renders
  inside <body>, which is where Cloudflare requires the beacon, so every page
  of the site is counted. Read the numbers in the Cloudflare dashboard under
  Web Analytics; blank cloudflare_analytics.token in _config.yml to switch it
  off.
{% endcomment %}
{% if site.cloudflare_analytics.token %}
  <!-- Cloudflare Web Analytics -->
  <script
    type="module"
    src="https://static.cloudflareinsights.com/beacon.min.js"
    data-cf-beacon='{"token": "{{ site.cloudflare_analytics.token }}"}'
  ></script>
  <!-- End Cloudflare Web Analytics -->
{% endif %}
